import pandas as pd
import numpy as np



class matrix_sample():

    def __init__(self):
        # TODO
        pass
        # data = pd.read_csv('../test/xx_4758prot_21sample.csv', header=0, index_col=0).T
        # sample=pd.read_csv('../test/sample_Info.csv',header=0)
        # data['merge']=data.index
        # sample['merge']=sample['Rawfile_name'].str.split('.',1,expand=True)[0]
        # # print(data,sample)
        # df=pd.merge(data,sample.loc[:,['type','merge']],on='merge',how='left')
        # print(df.columns)
        # df.index=df['type']
        # df=df.drop(axis=1,columns=['merge','type'])
        # df=df.T
        # print(df)
